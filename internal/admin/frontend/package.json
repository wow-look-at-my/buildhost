{
  "private": true,
  "scripts": {
    "build": "npm run check && esbuild src/app.ts --bundle --outfile=../static/app.js --format=iife --global-name=App --target=es2020 && esbuild src/copy.ts --bundle --outfile=../static/copy.js --format=iife --target=es2020",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
